---
import { cn } from '../../lib/utils/cn';

interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---
<div class={cn("bg-white p-6 rounded-2xl shadow-sm border border-slate-200 overflow-hidden", className)}>
  <slot />
</div>
