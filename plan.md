
1. Create a `validate-blog-orphans.mjs` script
2. Create a `validate-phase5-content-quality.mjs` script
3. Create a `validate-blog-media.mjs` script
4. Create a `validate-phase5-complete.mjs` script
5. Integrate all the validations in `package.json`
6. Integrate the new validators in CI workflow `.github/workflows/build.yml`
7. Ensure validations run effectively and no new errors are introduced
