# Content Preservation Map

- No files deleted (except a temporary `.json.ts` data endpoint previously mapped to old queries which we don't need for the static site generating hub).
- Modified files kept all components, simply migrated to standardized props via `normalizeBlogPost`.
