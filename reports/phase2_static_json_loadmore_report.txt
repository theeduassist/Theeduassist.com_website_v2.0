Static JSON Load More Report

- Created JSON endpoint `src/pages/data/blog/page-[page].json.ts` which uses `getStaticPaths` exactly identical to the blog page pagination (`pageSize: 12`).
- The endpoint maps out Sanity summaries into lightweight custom JSON structures.
- Updated `AllArticlesGrid.astro` to add a visually hidden `<template>` element containing the DOM structure for a single blog card.
- Replaced the previous CSS-toggling Javascript logic with an asynchronous `fetch` call pointing securely to `/data/blog/page-${nextPage}.json`.
- Implemented robust error handling, loading states, disable attributes, DOM cloning using `template.content.cloneNode(true)`, and `textContent` text injection to prevent XSS.
- Integrated an `aria-live="polite"` node to announce "X more articles loaded" for screen reader accessibility.
- Result: Users get instant feedback and pages without requiring any browser-side Sanity dependencies or full page loads.
