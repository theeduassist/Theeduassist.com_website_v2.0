# Mobile Overflow Review

- The existing `/blog/[slug]` and `/blog/category/[slug]` templates handle responsiveness.
- Tables included in new articles must be wrapped in an `overflow-x-auto` container to prevent mobile overflow. This will be enforced during the Sanity content authoring process.
