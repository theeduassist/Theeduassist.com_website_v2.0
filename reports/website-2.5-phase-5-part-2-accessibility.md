# Accessibility Review

- The new clusters rely on the existing `/blog/[slug]` template which passed baseline accessibility reviews (one H1, accessible landmarks, etc).
- Checklists and tables within the planned Sanity structure will use semantic HTML markup when rendered by the Portable Text engine.
- Images will require `alt` text to pass validation during the publishing stage.
